# Copy to .env and fill in. Production DATABASE_URL must point at a Canadian-region Postgres
# (data residency — plan §7.2/§8.6, ADR 0002).
DATABASE_URL="postgresql://USER:PASSWORD@HOST:PORT/DBNAME?schema=public"
